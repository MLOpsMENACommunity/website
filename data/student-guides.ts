export type StudentGuide = {
  slug: string
  title: string
  description: string
}

// Add published guides here. The page renders its empty state while this is empty.
export const studentGuides: StudentGuide[] = []
